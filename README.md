# Minesweeper Tilt

A static web game prototype that combines Minesweeper rules with a rolling steel ball controlled by device tilt.

## Local development

```bash
npm install
npm run dev
```

## Production build

```bash
npm run build
```

Netlify publishes the `dist` folder using the settings in `netlify.toml`.
